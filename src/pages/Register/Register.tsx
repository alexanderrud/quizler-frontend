import AuthForm from "../../components/form/AuthForm";

const Register = () => {
    return (
        <div>
            <AuthForm isRegister={true}/>
        </div>
    );
};

export default Register;